# Python (hook scripts are stdlib-only; no build artifacts should be tracked)
__pycache__/
*.py[cod]

# OS cruft
.DS_Store
Thumbs.db

# Ringmaster runtime ledger (per-project working state — never part of the plugin)
.ringmaster/
